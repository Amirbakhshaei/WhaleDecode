# Local secrets for `wrangler dev` (gitignored). Copy to `.dev.vars` and fill in.
# Production: use `wrangler secret put <NAME>` instead of committing.

BOT_TOKEN=
BOT_USERNAME=whaledecodebot
CHANNEL_CHAT_ID=
CHANNEL_PUBLISH_ENABLED=true
GROQ_API_KEY=
GROQ_BASE_URL=https://api.groq.com/openai/v1
GEMINI_API_KEY=
ALCHEMY_WEBHOOK_SIGNING_KEYS=
ALCHEMY_AUTH_TOKEN=
ALCHEMY_NOTIFY_TOKEN=
